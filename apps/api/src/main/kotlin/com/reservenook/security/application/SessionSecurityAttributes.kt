package com.reservenook.security.application

object SessionSecurityAttributes {
    const val AUTHENTICATED_AT_MILLIS = "security.authenticatedAtMillis"
    const val LAST_SEEN_AT_MILLIS = "security.lastSeenAtMillis"
    const val RECENT_AUTH_AT_MILLIS = "security.recentAuthAtMillis"
}
